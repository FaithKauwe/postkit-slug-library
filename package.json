{
  "name": "postkit-slug",
  "version": "1.0.0",
  "description": "Generate and validate URL-safe slugs for PostKit posts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm test && npm run build"
  },
  "keywords": ["slug", "url", "postkit"],
  "author": "Faith Kauwe",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
